- name: stop monit service
  service:
    name: delayed_job_{{ app }}
    state: stopped
  become: yes
  become_user: root

- name: remove monit package
  apt:
    pkg: monit
    state: absent
  become: yes
  become_user: root
